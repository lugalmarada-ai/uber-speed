package com.uberspeed.client

import android.app.Application

class UberSpeedApp : Application() {
    override fun onCreate() {
        super.onCreate()
        // Initialize things if needed
    }
}
